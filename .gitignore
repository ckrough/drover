# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/

# Virtual environments
.venv/
venv/
ENV/

# Python version (use pyproject.toml requires-python instead)
.python-version

# IDE
.vscode

# Testing & tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Environment
.env
.env.local

# OS
.DS_Store
Thumbs.db

# Temporary files
tmp/

# Debug output
debug/
*.prompt.txt
*.response.txt

# Evaluation framework
eval/documents/
eval/results/

# Real-world sample documents and ground truth (PII risk; local only)
eval/samples/real-world/*.pdf
eval/samples/real-world/*.docx
eval/samples/real-world/*.png
eval/samples/real-world/*.jpg
eval/samples/real-world/*.jpeg
eval/samples/real-world/*.tiff
eval/ground_truth/real-world.jsonl

# Per-doc benchmark JSON dumps (large; reconstructible by re-running)
eval/runs/*/*.json
eval/runs/*.json

# Integration tests (user-specific LLM config)
tests/integration/config.yaml

# Beads issue tracker
.beads/
